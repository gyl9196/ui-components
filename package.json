{
  "name": "yanlin",
  "version": "0.0.2",
  "devDependencies": {
    "lerna": "^3.18.3"
  },
  "scripts": {
    "publish": "lerna publish --yes --conventional-commits --registry https://registry.npmjs.org"
  },
  "workspaces": [
    "packages/*"
  ]
}
