{
  "name": "yanlin",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "publishmodules": "lerna publish --yes --conventional-commits --registry https://registry.npmjs.org"
  },
  "devDependencies": {
    "lerna": "^3.18.3"
  },
  "workspaces": [
    "packages/*"
  ]
}
